<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .navbar-brand {
            font-weight: bold;
        }
        .navbar-nav .nav-link {
            color: #0d6efd;
        }
        .navbar-nav .nav-link:hover {
            text-decoration: underline;
        }
        .navbar-nav .nav-link.active {
            background-color: #0d6efd;
            color: white !important;
            border-radius: 0.25rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg border-bottom">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Header</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link <%= pathname === '/' ? 'active' : '' %>" href="/">Home</a></li>
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link <%= pathname === '/members' ? 'active' : '' %>" href="/members">Members</a></li>
                        <li class="nav-item"><a class="nav-link <%= pathname === '/logout' ? 'active' : '' %>" href="/logout">Logout</a></li>
                        <% if (user.user_type === 'admin') { %>
                            <li class="nav-item"><a class="nav-link <%= pathname === '/admin' ? 'active' : '' %>" href="/admin">Admin</a></li>
                        <% } %>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link <%= pathname === '/login' ? 'active' : '' %>" href="/login">Login</a></li>
                    <% } %>
                    <li class="nav-item"><a class="nav-link <%= pathname === '/404' ? 'active' : '' %>" href="/404">404</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-4">